#import "lib/template.typ": template
#show: template

= BQL Tools

Online hosted tools for dealing with BQL.

== Schema browsers

- #link("/bql/schema-viewer.html?schema=schema/small-schema.json")[Small schema]
- #link("/bql/schema-viewer.html?schema=schema/base-schema.json")[Base schema]. Very large (180k hooks). Takes a moment to load
